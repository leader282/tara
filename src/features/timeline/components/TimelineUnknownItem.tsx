import { AppText } from "@/components/ui";

export function TimelineUnknownItem() {
  return (
    <AppText color="textSecondary" variant="caption">
      This shared moment is saved safely.
    </AppText>
  );
}
